---
name: 📦 Create Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build & Release VSIX
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: 🔍 Checkout Code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: ⚙️ Setup and Build
        uses: ./.github/actions/setup-build

      - name: 🧪 Validate Tests
        run: |
          xvfb-run -a npm run test
          xvfb-run -a npm run test:e2e

      - name: 🏷️ Determine Version and Tag
        id: vars
        run: |
          VERSION=$(node -p "require('./package.json').version")
          TAG="v$VERSION"
          VSIX="gherkin-powertools-$VERSION.vsix"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "vsix=$VSIX" >> "$GITHUB_OUTPUT"

      - name: 📦 Package VSIX
        run: |
          npx vsce package --out ${{ steps.vars.outputs.vsix }}
          npx vsce ls

      - name: 🛡️ Verify VSIX
        run: |
          if [ ! -s "${{ steps.vars.outputs.vsix }}" ]; then
            echo "Error: VSIX file is missing or empty!"
            exit 1
          fi

      - name: 🏷️ Ensure Git Tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.vars.outputs.tag }}"
          if git ls-remote --tags origin "refs/tags/$TAG" | grep -q "$TAG"; then
            echo "Tag $TAG already exists."
          else
            echo "Creating tag $TAG..."
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git tag "$TAG"
            git push origin "$TAG"
          fi

      - name: 🚀 Ensure GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.vars.outputs.tag }}"
          if gh release view "$TAG" > /dev/null 2>&1; then
            echo "Release $TAG already exists."
          else
            echo "Creating release $TAG..."
            gh release create "$TAG" --generate-notes
          fi

      - name: 📎 Upload VSIX Asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.vars.outputs.tag }}"
          VSIX="${{ steps.vars.outputs.vsix }}"
          if gh release view "$TAG" --json assets -q ".assets[].name" | grep -qx "$VSIX"; then
            echo "Asset $VSIX already uploaded to release $TAG."
          else
            echo "Uploading asset $VSIX to release $TAG..."
            gh release upload "$TAG" "$VSIX" --clobber
          fi

      - name: 🌐 Publish to VS Code Marketplace
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: |
          if [ -z "$VSCE_PAT" ]; then
            echo "VSCE_PAT secret is missing. Skipping marketplace publish."
            exit 0
          fi
          echo "Publishing to Visual Studio Marketplace..."
          npx vsce publish -p $VSCE_PAT
