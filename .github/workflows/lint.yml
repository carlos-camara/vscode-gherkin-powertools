---
name: 🛡️ Code Quality Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write # Required for lint-codebase to update the PR checklist
  statuses: write # Required for Super-Linter to post commit statuses

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & Compile
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: 🔍 Checkout Code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: ⚙️ Setup and Build
        uses: ./.github/actions/setup-build

      - name: 📋 Check Configuration Sync
        run: npm run check:config

      - name: 📦 Validate Marketplace Metadata
        run: npx vsce ls

      - name: 🧹 Lint Codebase (Markdown, YAML, Actions)
        uses: carlos-camara/qa-hub-actions/lint-codebase@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
