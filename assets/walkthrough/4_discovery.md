# 🔍 Python Behave Discovery

Do you keep your Python step definitions in a custom folder?

By default, we look inside `**/steps/**/*.py` and `**/features/steps/**/*.py`. 

If your repository has a unique structure, you must update the **Step Globs** setting so the extension can parse your Python functions and build the navigation cache.
