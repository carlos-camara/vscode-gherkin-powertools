# 🧭 Semantic Go to Definition

Navigate massive test suites instantly.

Right-click any Gherkin step and select **Go to Definition** (`F12`).
The extension uses semantic matching to parse `@given`, `@when`, and `@then` decorators and instantly jumps you to the correct Python file.

*Tip: It automatically resolves ambiguous `And` and `But` steps!*
